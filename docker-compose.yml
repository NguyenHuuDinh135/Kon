services:
  db:
    image: ankane/pgvector:latest
    container_name: kon_postgres
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER:-nguyenhuudinh}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-kon_password}
      POSTGRES_DB: ${DB_NAME:-kon_erp_northwind}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-nguyenhuudinh} -d ${DB_NAME:-kon_erp_northwind}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - kon_network

  worker:
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    container_name: kon_worker
    restart: on-failure
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./packages:/app/packages
      - ./apps/worker:/app/apps/worker
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql://${DB_USER:-nguyenhuudinh}:${DB_PASSWORD:-kon_password}@db:5432/${DB_NAME:-kon_erp_northwind}
      - PYTHONPATH=/app/packages/db-core:/app/packages/shared:/app/packages/ai-engine:/app/packages/mcp-servers
      - PYTHONUNBUFFERED=1
      - AWS_REGION=${AWS_REGION:-us-west-2}
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - AWS_SESSION_TOKEN=${AWS_SESSION_TOKEN:-}
    deploy:
      resources:
        limits:
          memory: 2G
    depends_on:
      db:
        condition: service_healthy
    networks:
      - kon_network

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    container_name: kon_api
    restart: unless-stopped
    command: uvicorn apps.api.main:app --host 0.0.0.0 --port 8000 --reload
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8000:8000"
    volumes:
      - ./packages:/app/packages
      - ./apps/api:/app/apps/api
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql://${DB_USER:-nguyenhuudinh}:${DB_PASSWORD:-kon_password}@db:5432/${DB_NAME:-kon_erp_northwind}
      - PYTHONPATH=/app/packages/db-core:/app/packages/shared:/app/packages/ai-engine:/app/packages/mcp-servers:/app/apps/api
      - PYTHONUNBUFFERED=1
      - AWS_REGION=${AWS_REGION:-us-west-2}
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - AWS_SESSION_TOKEN=${AWS_SESSION_TOKEN:-}
    healthcheck:
      test: ["CMD-SHELL", "python3 -c 'import urllib.request; urllib.request.urlopen(\"http://localhost:8000/health\")'"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 10s
    depends_on:
      db:
        condition: service_healthy
    networks:
      - kon_network

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    container_name: kon_web
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
      - INTERNAL_API_URL=http://api:8000
      - HOSTNAME=0.0.0.0
    depends_on:
      api:
        condition: service_healthy
    networks:
      - kon_network

networks:
  kon_network:
    driver: bridge

volumes:
  postgres_data:
